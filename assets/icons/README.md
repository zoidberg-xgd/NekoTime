# 图标资源说明

## 源文件

- **`app_icon_source.png`** - 主图标源文件
  - 用于生成所有平台的应用图标和托盘图标
  - 建议尺寸: 1024x1024 或更大
  - 格式: PNG，支持透明背景

## 生成的图标

从 `app_icon_source.png` 自动生成以下图标：

### 应用图标
- **macOS**: `macos/Runner/Assets.xcassets/AppIcon.appiconset/`
  - 多尺寸: 16x16, 32x32, 64x64, 128x128, 256x256, 512x512, 1024x1024
- **Windows**: `windows/runner/resources/app_icon.ico`
  - 多尺寸 ICO 文件
- **Linux**: `linux/runner/resources/app_icon.png`
  - 单个 PNG 文件

### 系统托盘图标
- **`tray_icon.ico`** - Windows 系统托盘图标
  - 多尺寸: 16x16, 32x32, 48x48, 64x64, 128x128, 256x256
  - 用于 Windows 11 任务栏托盘区域显示

## 重新生成图标

### 方法 1: 使用自动化脚本（推荐）

```bash
# 从项目根目录运行
./scripts/generate_icons.sh
```

这个脚本会：
1. 使用 `flutter_launcher_icons` 生成所有应用图标
2. 使用 `ImageMagick` 生成托盘图标

**依赖要求:**
- Flutter (已安装 `flutter_launcher_icons` 包)
- ImageMagick (可选，用于生成 .ico)
  ```bash
  # macOS
  brew install imagemagick
  
  # Linux
  sudo apt install imagemagick
  
  # Windows
  choco install imagemagick
  ```

### 方法 2: 手动生成

#### 1. 应用图标
```bash
dart run flutter_launcher_icons
```

#### 2. 托盘图标（如果没有 ImageMagick）
在线转换工具:
- https://convertio.co/png-ico/
- https://www.icoconverter.com/

上传 `app_icon_source.png`，生成 `.ico` 文件，保存为 `tray_icon.ico`

## 修改图标

1. **准备新图标**
   - 创建或修改 `app_icon_source.png`
   - 建议使用矢量图形工具 (如 Figma, Sketch, Illustrator)
   - 导出为 PNG，至少 1024x1024

2. **替换源文件**
   ```bash
   cp your-new-icon.png assets/icons/app_icon_source.png
   ```

3. **重新生成所有图标**
   ```bash
   ./scripts/generate_icons.sh
   ```

4. **提交更改**
   ```bash
   git add assets/icons/ macos/ windows/ linux/
   git commit -m "chore: update app icons"
   ```

## 设计建议

### 图标要求
- ✅ 简洁明了，易于识别
- ✅ 在小尺寸（16x16）下清晰可见
- ✅ 在浅色和深色背景下都清晰
- ✅ 支持透明背景
- ❌ 避免过多细节（在小图标上会模糊）
- ❌ 避免细线条（在缩放时可能丢失）

### 平台特点
- **macOS**: 圆角正方形，渐变和阴影效果受欢迎
- **Windows**: 现代简洁，遵循 Fluent Design
- **Linux**: 扁平化设计，清晰的轮廓

### 托盘图标
- 保持极简设计
- 在 16x16 尺寸下必须清晰可辨
- 考虑浅色和深色系统主题
- Windows 11 特别要求实际的 .ico 文件

## 当前图标信息

- **源图标**: 842B (很小，可能需要更高分辨率)
- **托盘图标**: 72KB (包含多尺寸)
- **应用图标**: 自动生成多尺寸

💡 **建议**: 更新 `app_icon_source.png` 为更高分辨率的版本以获得更好的显示效果。
